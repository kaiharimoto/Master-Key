# Signing config for release builds.
#
# These credentials are deliberately committed. See KEYS.md before changing
# ANYTHING in this file — regenerating the key permanently breaks in-app updates
# for the installed app.
storeFile=keystore/master-key-release.jks
storePassword=masterkey-release
keyAlias=masterkey
keyPassword=masterkey-release
